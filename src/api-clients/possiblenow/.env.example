# PossibleNOW API Configuration
# Copy this file to .env and fill in your actual values

# Required: OAuth 2.0 Credentials
POSSIBLENOW_CLIENT_ID=your-client-id-here
POSSIBLENOW_CLIENT_SECRET=your-client-secret-here

# Optional: Environment (sandbox or production)
# Default: sandbox
POSSIBLENOW_ENV=sandbox

# Optional: OAuth Scope
# Default: dnc:read dnc:write
POSSIBLENOW_SCOPE=dnc:read dnc:write

# Optional: Request timeout in milliseconds
# Default: 30000 (30 seconds)
POSSIBLENOW_TIMEOUT=30000

# Optional: Maximum number of retries for failed requests
# Default: 3
POSSIBLENOW_MAX_RETRIES=3

# Optional: Rate limits
# These should match your PossibleNOW account limits
POSSIBLENOW_RPS=10        # Requests per second
POSSIBLENOW_RPM=100       # Requests per minute
POSSIBLENOW_RPH=5000      # Requests per hour

# Optional: Webhook configuration (if using async results)
# POSSIBLENOW_WEBHOOK_URL=https://your-domain.com/webhooks/possiblenow
# POSSIBLENOW_WEBHOOK_SECRET=your-webhook-secret